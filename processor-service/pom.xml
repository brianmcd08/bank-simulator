<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.bankcorp</groupId>
    <artifactId>bank-simulator</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>processor-service</artifactId>
  <name>processor-service</name>
  <description>Receives bank messages over HTTP, routes them through the topic and queues, and audits outcomes.</description>

  <dependencies>
    <!-- Embedded Tomcat plus @RestController support. Brings Jackson 3 with it. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc</artifactId>
    </dependency>

    <!-- RabbitTemplate, to publish outcomes to the broker. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-amqp</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <mainClass>com.bankcorp.banksim.ProcessorApplication</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
